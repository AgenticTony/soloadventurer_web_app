---
description: One iteration of the standing maintenance loop — triage → implementer → verifier → PR (defined in loop.md)
---

Read `loop.md` at the repo root, then run **one iteration** of the standing maintenance loop exactly as it specifies — the triage → implementer → verifier → PR flow, its stop conditions, and its hard guardrails.

Non-negotiable guardrail (restated from loop.md): **you never merge** — every PR the loop opens waits for human review. Never run `gh pr merge`.
